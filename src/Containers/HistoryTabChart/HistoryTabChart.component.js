// @flow
import * as React from 'react';
import { View, Text } from 'react-native';

export const HistoryTabChart = (): React.Node => {
  return (
    <View>
      <Text>History tab chart</Text>
    </View>
  );
};

export default HistoryTabChart;
